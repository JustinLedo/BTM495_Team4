/* CREATE TABLE employees
(
id INT PRIMARY KEY IDENTITY (1,1), 
username VARCHAR(MAX) NULL,
password VARCHAR(MAX) NULL,
)
INSERT INTO employees (username, password) VALUES ('testE', 'test'); */

SELECT * FROM employees